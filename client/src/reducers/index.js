import {} from '../actions'

const initialState = {
    activities: [],
    countriesPaginated: [],
    countriesNotPaginated: [],
    filterOnFront: false,
    page: 1    
};

function rootReducer(state = initialState, action) {
}
  
export default rootReducer;